// src/reportWebVitals.js

const reportWebVitals = (metric) => {
  // Log the performance metrics to the console (you can replace this with any reporting tool)
  console.log(metric);
};

export default reportWebVitals;
